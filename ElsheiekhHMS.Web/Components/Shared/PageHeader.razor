<header class="hms-page-header">
    @if (!string.IsNullOrWhiteSpace(Eyebrow))
    {
        <p class="hms-eyebrow">@Eyebrow</p>
    }
    <div class="hms-page-header__row">
        <div>
            <h1 id="@HeadingId" tabindex="-1">@Title</h1>
            @if (!string.IsNullOrWhiteSpace(Description))
            {
                <p class="hms-page-header__description">@Description</p>
            }
        </div>
        @if (Actions is not null)
        {
            <div class="hms-page-header__actions">@Actions</div>
        }
    </div>
</header>

@code {
    [Parameter, EditorRequired] public string Title { get; set; } = string.Empty;
    [Parameter] public string? Description { get; set; }
    [Parameter] public string? Eyebrow { get; set; }
    [Parameter] public string HeadingId { get; set; } = "page-heading";
    [Parameter] public RenderFragment? Actions { get; set; }
}
